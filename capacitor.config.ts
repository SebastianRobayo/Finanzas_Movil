import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Finanzas_Movil',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
